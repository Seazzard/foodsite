import React from 'react';
import RouteContent from './RouteContent/RouteContent';
import Logo from '../../../../assets/icons/Sets.png';

const Sets = () => {
  return <RouteContent Logo={Logo} title={'Сеты'} path={'sets'} />;
};

export default Sets;
